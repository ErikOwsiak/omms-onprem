

class logProxy(object):

   @staticmethod
   def log_exp(ex):
      print(ex)

   @staticmethod
   def log_err(ex):
      print(ex)

   @staticmethod
   def log_warning(msg):
      print(f"WARNING:: [ {msg} ]")
